import React from 'react';
import { Button } from '@material-ui/core';

function App() {
	return (
		<div>
			<Button color="primary" variant="contained">
				Click Me
			</Button>
		</div>
	);
}

export default App;
